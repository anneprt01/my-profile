# Mascotte

Déposer ici le fichier fourni dans le brief :

- `horse_final.png` — cheval-jockey pixel art, fond transparent, pose de saut.

Tant que le fichier n'est pas là, le site retombe automatiquement sur un
émoji 🐎 (voir `src/components/SafeImage.jsx`) — rien ne casse en son
absence.
